<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sqllite">

    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EarthQuakeApplication">
        <activity
            android:name=".SearchTestActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.default_searchable"
                android:value=".ContentProviderTestActivity" />
        </activity>
        <activity
            android:name=".ContentProviderTestActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/hoard_search" />
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <provider
            android:name=".provider.MyHoardContentProvider"
            android:authorities="com.example.sqllite.provider.hoarder" />
    </application>

</manifest>